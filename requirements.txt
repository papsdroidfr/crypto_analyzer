# CryptoAnalyzer — dépendances Python
# Compatible Raspberry Pi 4 (ARM64) avec Python 3.11+

# Manipulation de données
polars==0.20.31

# Requêtes HTTP
requests==2.32.3

# Graphiques (backend Agg non-interactif pour headless)
matplotlib==3.9.0
numpy==1.26.4

# Utilitaires (optionnel mais recommandé)
python-dateutil==2.9.0

# Outils de test
pytest>=7.0.0
# pytest-mock 4.x may not be available on all platforms; pin to 3.x
pytest-mock>=3.8.0
coverage>=7.0.0
